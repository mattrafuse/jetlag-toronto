// ── React-based sidebar UI entry ───────────────────────────────
// The SidebarPanel is now mounted by overlay.tsx in a shared
// React root. This module just re-exports what sidebar.ts needs.

export { store } from "./store";
